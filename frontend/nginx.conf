server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Required for React Router — send all routes to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
